const fs = require('fs');
const path = require('path');

// fs.open('text2.js','w', (err)=>{
//     if(err) throw err;
//     console.log('saved');
// })

const baseDir = path.join(__dirname, '../helpers/')

console.log('hello', path);