// dependencies
const {sampleRouteHandler} = require('./handlers/routeHandlers/sampleRouteHandler')

// module scaffolding
const routes = {
    sample : sampleRouteHandler,
};

module.exports =  routes;